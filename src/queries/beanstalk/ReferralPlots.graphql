query ReferralPlots(
  $farmer: String!
  $first: Int = 1000
  $skip: Int = 0
) {
  plots(
    first: $first
    skip: $skip
    where: { 
      farmer: $farmer
      source: REFERRAL
    }
  ) {
    id
    pods
    index
    farmer {
      id
    }
  }
}
